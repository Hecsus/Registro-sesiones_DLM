<!DOCTYPE html>
<html lang="es">
  <%- include("partials/head") %>

  <body>
    <%- include("partials/header") %>

    <section class="container py-5">
      <h2 class="mb-4 text-center"><%= title %></h2>

      <!-- ⚠️ Muestra error si hay -->
      <% if (errorMsg) { %>
        <div class="alert alert-danger"><%= errorMsg %></div>
      <% } %>

      <!-- 🧾 Formulario para crear o editar producto -->
      <form action="<%= accion %>" method="<%= metodo %>" novalidate>
        <div class="mb-3">
          <label for="ref" class="form-label">Referencia</label>
          <input
            type="text"
            class="form-control"
            id="ref"
            name="ref"
            value="<%= producto.ref || '' %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="nombre" class="form-label">Nombre del producto</label>
          <input
            type="text"
            class="form-control"
            id="nombre"
            name="nombre"
            value="<%= producto.nombre || '' %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="precio" class="form-label">Precio (€)</label>
          <input
            type="number"
            class="form-control"
            id="precio"
            name="precio"
            step="0.01"
            value="<%= producto.precio || '' %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="stock" class="form-label">Stock disponible</label>
          <input
            type="number"
            class="form-control"
            id="stock"
            name="stock"
            value="<%= producto.stock || '' %>"
            required
          />
        </div>

        <!-- Botón principal -->
        <button type="submit" class="btn btn-primary w-100"><%= botonTexto %></button>

        <!-- Enlace para cancelar y volver -->
        <div class="text-center mt-3">
          <a href="/admin">← Volver al panel</a>
        </div>
      </form>
    </section>

    <%- include("partials/footer") %>
  </body>
</html>
